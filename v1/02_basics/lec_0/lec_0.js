let a = [1, 2, 4, 5, 7];
// console.log(a);
// console.log(a[0]);
// console.log(a[2]);
// console.log(a[4]);

// let b = a;
// b[0] = 'A';
// console.log(a); // [ 'A', 2, 4, 5, 7 ]

// console.log(a.length);

// console.log(typeof a); // object

// a[0] = "A";
// console.log(a); // [ 'A', 2, 4, 5, 7 ]

let c = [1,2,3,4,5,6,7,8,9];
// console.log(c.pop()); // Removes the last element from an array and returns it.
// console.log(c);

// console.log(c.push(10)); // Appends new elements to the end of an array, and returns the new length of the array.
// console.log(c);

// console.log(c.unshift(0)); // Inserts new elements at the start of an array, and returns the new length of the array.
// console.log(c);

// console.log(c.shift()); // Removes the first element from an array and returns it.
// console.log(c);

// delete c[0];
// console.log(c);

// console.log(c.includes(9));

// console.log(c.indexOf(0)); // Returns the index of the first occurrence of a value in an array, or -1 if it is not present.

// console.log(a.toString()); // 1,2,4,5,7
// console.log(a.join("->")); // 1->2->4->5->7

let d = [1,2,3,4,5];
// console.log(d.splice(2, 2, 'a', 'b', 'c')); // Removes elements from an array and, if necessary, inserts new elements in their place, returning the deleted elements. It manipulates the original array.
// console.log(d); // [ 1, 2, 'a', 'b', 'c', 5 ]
// console.log(d.slice(2,4)); // Returns a copy of a section of an array. It doesn't manipulate the original array.
// console.log(d);
